services:
  agent:
    build: .
    ports:
      - "8088:8088"
    env_file:
      - .env
    environment:
      # Override or provide defaults on top of .env values.
      - AGENT_NAME=${AGENT_NAME:-code-helper}
